# vim-vtools

v language tools for vim

## Features

- Auto format

## Commands

- Fmt: v fmt

## Configuration

```
" enable auto format when write. (defaut)
let g:vfmt = 1
" disable auto format.
let g:vfmt = 0
```
